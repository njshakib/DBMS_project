-- =========================================================
-- CoalFieldSupplies table -- FULL DATA (1 rows)
-- 1 field-to-plant supply link (Barapukuria field to Barapukuria plant) -- depends on CoalField, CoalPowerPlant
-- =========================================================

USE energy_data;

INSERT INTO CoalFieldSupplies (field_id, plant_id) VALUES
(1, 1);

